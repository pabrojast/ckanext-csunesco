fecha: 2026-07-16 19:23:19
tarea: Plugin CKAN ckanext-csunesco Fase 1 (registro CS sin org, proyectos/landing/listado, panel aprobacion, news/events, integracion ofform) - COMPLETA
verify: pass (ckan-load+pytest+build+harness)
iter: 12/12

## Resumen
## Última corrida (/mix plan) — Fase 1 Citizen Science: COMPLETA y verificada fuertemente

Tarea: plugin CKAN ckanext-csunesco estilo water-family + registro CS sin org + landing/listado proyectos +
panel aprobacion + news/events desde ofform a IHP-WINS + join con aprobacion; diseño UNESCO. Data analyzer/AI = Fase 2.

Estado final: plan aprobado (.mix/plan.md) + 12 iteraciones de build, TODAS con verificacion fuerte (pass).
- ckanext-csunesco (/home/pabrojast/Proyectos/ckanext-csunesco): registro CS web+API, seed 4 iniciativas,
  dominio (18 actions csunesco_*), landing/listado/mapa/QR/at-a-glance, panel admin, news/events (cs_content),
  nav wiring. **CARGA EN CKAN 2.10 REAL** (docker ckan/ckan-dev:2.10) + 79 tests conductuales (ORM/mapper/DDL,
  validators, sanitize) PASAN. Fix real cazado por verificacion conductual: ckanext/__init__.py necesitaba
  declare_namespace (no pkgutil.extend_path) para pip install -e con namespace_packages. Harness reproducible:
  Dockerfile.test + scripts/run-ckan-tests.sh + docker-compose.dev.yml. Docs: README, INSTALL.md, docs/OFFORM_INTEGRATION.md.
- ofform backend: outbox idempotente (cs_sync.py), reconcile CKAN->ofform (cs_reconcile.py), retry-sync,
  POST /cs/register (dual-write CKAN-first, CsRegistrationService), Programme espejo, Submission.programme_id.
  20 tests pytest PASAN (sync/reconcile/register, caplog password OK).
- ofform frontend: registro funcional, proyectos/join/dashboard, panel aprobacion, editor news/events,
  RegionMapPicker, SyncStatusBadge con retry, i18n 5 locales, dark/RTL. tsc OK + vitest 144 passed + vite build.

Decision de identidad tomada: PROXY CONFIABLE con dual-write (registro crea user CKAN + local ofform),
en vez de JIT/session-bridge. Token de servicio server-side. HMAC per-user = hardening futuro.

PENDIENTE (Fase 2 / hardening opcional): data viewer/analyzer + AI insights; test HTTP end-to-end con stack
CKAN completo levantado (postgres+solr) — el harness docker-compose.dev.yml queda listo; email verification;
tokens per-user HMAC-firmados. Ningun commit/push (ofform deploya por commit a master; el usuario decide cuando).
